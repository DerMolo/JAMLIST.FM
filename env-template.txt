# Copy this entire file content into your .env file
# Then replace the placeholder values with your actual credentials

# Database Connection (replace 'your_password' with your PostgreSQL password)
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/spotifyapp"

# Spotify Credentials (get these from https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID="your_client_id_from_spotify"
SPOTIFY_CLIENT_SECRET="your_client_secret_from_spotify"

# App URL (use 127.0.0.1 NOT localhost - Spotify requirement)
NEXTAUTH_URL="http://127.0.0.1:3000"

# Security Secrets (generate using ssl) 
NEXTAUTH_SECRET="<hash taken from openssl rand -base64 32>"
JWT_SECRET="<hash taken from openssl rand -base64 32>"

# ======================================
# WHAT TO REPLACE:
# ====================================================================
# 1. "your_password" → Your PostgreSQL password
# 2. "your_client_id_from_spotify" → Client ID from Spotify Dashboard
# 3. "your_client_secret_from_spotify" → Client Secret from Spotify Dashboard
# 4. Keep everything else as-is!
# ====================================================================

